import { DEFAULT_FRAME_SEQUENCE } from './scroll-frames/config'

export const DEFAULT_FRAMES = { ...DEFAULT_FRAME_SEQUENCE }

export const DEFAULT_SCROLL = {
  scrollLength: '+=130%',
  scrub: 0.4,
  scaleFrom: 1.06,
  vignetteFrom: 0.45,
  vignetteTo: 0.2,
}
